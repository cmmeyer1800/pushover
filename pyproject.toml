[project]
name = "pushover-python"
version = "0.1.0"
description = "Pushover notificaton service python API client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
]
